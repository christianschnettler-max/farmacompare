// Función eliminada — asistente IA removido para evitar costos por uso.
export default async () => Response.json({ error: "No disponible" }, { status: 404 });
export const config = { path: "/api/chat" };
